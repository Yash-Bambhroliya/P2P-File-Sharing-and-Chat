// src/Particles.js
import React, { useEffect } from 'react';
import './Particles.js'; // Ensure the casing matches the file name

const Particles = () => {
  useEffect(() => {
    // Any additional setup for particles.js can be done here
  }, []);

  return <div id="particles-js"></div>;
};

export default Particles;
